---
import type { ImageMetadata } from 'astro';
import { Image } from 'astro:assets';

interface Props {
  team: { name: string; flag: string };
  bgClass: string;
  nameClassName?: string;
}

const { team, bgClass, nameClassName = "" } = Astro.props;

import { resolveFlag } from '../lib/flags';
---

<div class="flex flex-col items-center group/team-inner">
  <div class={`w-28 h-28 md:w-64 md:h-64 rounded-full flex items-center justify-center shadow-2xl relative z-10 overflow-hidden border border-white/25 p-2 md:p-4 transition-all duration-700 group-hover/team-inner:border-white/30`}>
    <div class="w-full h-full rounded-full overflow-hidden shadow-inner">
      {team.flag.endsWith('.png') ? (
        <Image src={resolveFlag(team.flag) as ImageMetadata} alt={team.name} class="w-full h-full object-cover" />
      ) : (
        <span class="text-6xl md:text-8xl">{team.flag}</span>
      )}
    </div>
  </div>
  <h2 class={`text-2xl md:text-5xl font-bebas text-white uppercase tracking-[4px] mt-8 transition-all duration-500 group-hover/team-inner:text-gold group-hover/team-inner:scale-110 ${nameClassName}`}>
    {team.name}
  </h2>
</div>
